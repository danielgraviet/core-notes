# macOS
.DS_Store
.AppleDouble
.LSOverride

# Xcode build artifacts — never commit these
DerivedData/
build/
*.pbxuser
*.mode1v3
*.mode2v3
*.perspectivev3
*.xcuserstate

# Xcode per-user settings — machine-specific, not shared
xcuserdata/

# Swift Package Manager
.build/
.swiftpm/

# Playgrounds
timeline.xctimeline
playground.xcworkspace

# CocoaPods (not using it, but just in case)
Pods/

# Learning/scratch files that aren't part of the app build
core-notes/hello_world.swift
